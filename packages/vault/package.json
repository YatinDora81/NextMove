{
  "name": "@repo/vault",
  "version": "0.0.0",
  "private": true,
  "description": "The end-to-end profile-vault codec (JF-001 SEC 7.4 / 9.2), shared byte-for-byte by apps/web and apps/extension so a vault sealed in the browser opens in the service worker.",
  "type": "module",
  "license": "ISC",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "check-types": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^9.34.0",
    "typescript": "5.9.2",
    "vitest": "^4.1.10"
  }
}
